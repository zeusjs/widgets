# Save as .codeclimate.yml (note leading .) in project root directory
languages:
  JavaScript: true

exclude_paths:
    - "dist/*"
    - "docs/*"
    - ".gen/*"
    - ".tmp/*"
    - "node_modules/*"
    - "externs/*"
    - "Gruntfile.js"
    - "karma.conf.js"
